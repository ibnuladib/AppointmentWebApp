@model List<string>

<h2>Activity Logs</h2>

@if (Model.Any())
{
    <ul>
        @foreach (var log in Model)
        {
            <li>@log</li>
        }
    </ul>
}
else
{
    <p>No activity logged yet.</p>
}
